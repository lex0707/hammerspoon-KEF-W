[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oura-stats"
version = "0.1.0"
description = "Collect Oura Ring data and store it as CSV files"
readme = "README.md"
authors = [{ name = "AutoGenerated" }]
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[project.scripts]
oura-stats = "oura_stats.cli:main"
